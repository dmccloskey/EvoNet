### CMake EvoNet config file for external code
### configured by the EvoNet build system from <EvoNet>/cmake/EvoNetConfig.cmake.in

# we need this to reference the target file
get_filename_component(EVONET_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

# include directories for targets
@_EXPORT_INCLUDE_BLOCK@

set(EVONET_ADDCXX_FLAGS "@CF_EVONET_ADDCXX_FLAGS@")

## The targets file
include("${EVONET_CMAKE_DIR}/@_EVONET_EXPORT_FILE@")
